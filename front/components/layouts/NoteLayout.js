import React from 'react';

import NoteLayoutHeader from '../NoteLayoutHeader';
import NoteLayoutContent from '../NoteLayoutContent';

const NoteLayout = () => {
    return (
        <>
          <NoteLayoutHeader />
          <NoteLayoutContent/>
        </>
    );
}

export default NoteLayout;